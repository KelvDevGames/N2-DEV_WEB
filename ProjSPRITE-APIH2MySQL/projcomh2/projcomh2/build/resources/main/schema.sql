DROP TABLE IF EXISTS SPRITE;

CREATE TABLE SPRITE(
ID_SPRITE BIGINT AUTO_INCREMENT,
NOME_SPRITE VARCHAR(100) NOT NULL,
TAMANHO_SPRITE long NOT NULL,
VALOR_SPRITE double NOT NULL,
PRIMARY KEY (ID_SPRITE));
